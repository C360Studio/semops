## 1. OpenSpec Planning

- [x] 1.1 Create `semlink-live-companion-demo-ingest` as the next SemOps change after the deterministic
      `semlink-multi-companion-cop-demo` archive.
- [x] 1.2 Record the mixed ladder: one SITL-backed SemLink node first, `n >= 3` SemLink nodes for fleet proof, and
      `n` SITL instances only when claiming `n` live ArduPilot vehicles.
- [x] 1.3 Preserve SemLink ownership of SITL/runtime orchestration and SemOps ownership of artifact ingest and COP
      glass.
- [ ] 1.4 Run strict OpenSpec validation for the new change and all specs.

## 2. Generated Artifact Ingest

- [ ] 2.1 Define the SemOps ingest surface for SemLink-generated companion demo artifacts: file/CLI import or
      equivalent local handoff before any long-running cross-repo orchestration.
- [ ] 2.2 Record required artifact metadata: SemLink report kind, generated time, SemLink source/version or commit when
      available, generator command or profile, simulator family, vehicle profile, no-transmit posture, and assertion
      summary.
- [ ] 2.3 Reject unsupported report kinds, malformed generated artifacts, stale artifacts when a freshness policy is
      configured, and artifacts that claim SITL/hardware fidelity without required source metadata.
- [ ] 2.4 Add tests that distinguish committed fixture evidence from SemLink-generated artifact evidence.

## 3. Mixed SITL/Fleet View Model

- [ ] 3.1 Extend the companion-fleet view model with source-fidelity labels such as `fixture`, `generated`,
      `deterministic`, `sitl-backed`, and future `hardware-adjacent`.
- [ ] 3.2 Represent per-node live-source posture so the UI can say "three SemLink nodes, one SITL-backed" without
      implying three live ArduPilot vehicles.
- [ ] 3.3 Preserve ArduPilot SITL evidence as MAVLink/ArduPilot compatibility proof and avoid BlueOS-only assumptions.
- [ ] 3.4 Keep readback adapter status, `COMMAND_ACK`, and `AUTOPILOT_VERSION` result evidence separate when generated
      artifacts include them.

## 4. Browser And Demo Smoke

- [ ] 4.1 Add a SemOps smoke artifact copied from or generated by SemLink's live/SITL e2e lane.
- [ ] 4.2 Add browser or component smoke that imports that artifact and proves the COP shows at least three SemLink
      nodes with one explicit SITL-backed node.
- [ ] 4.3 Ensure the operator surface labels the evidence as SemLink-generated and SITL-backed where applicable, not as
      BlueOS, Navigator, hardware, radio, or `n` live-vehicle evidence.
- [ ] 4.4 Keep command execution, mission controls, peer management, mesh topology controls, BlueOS controls, and raw
      MAVLink inspection out of the MVP UI surface.

## 5. Boundary And Evidence Review

- [ ] 5.1 Record the condition under which `n` SITL instances become required: only for a claim of `n` live ArduPilot
      vehicles or per-node live vehicle fidelity.
- [ ] 5.2 Recheck SemConnect/CS API edge impact after generated artifact ingest exists; it should remain projection,
      not hot-path runtime.
- [ ] 5.3 Run an adversarial review before claiming the demo proves live SemLink/SITL integration.
- [ ] 5.4 Run `go test ./...`, `go build ./...`, `npm run check`, `npm run test`, relevant browser smoke, and
      `openspec validate --all --strict` when implementation lands.
