{
	auto_https off
}

:8080 {
	encode zstd gzip

	@semops_api {
		path /api/* /healthz /metrics
	}
	reverse_proxy @semops_api semops:8088

	@scenario_status {
		path /scenario/status
	}
	reverse_proxy @scenario_status semops-scenario-runner:8090

	reverse_proxy semops-ui:3000
}
